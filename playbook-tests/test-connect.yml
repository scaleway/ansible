---
- name: Test connection to Scaleway hosts
  hosts: all
  gather_facts: no
  tasks:
    - name: Ping host
      ping:

    - name: Show host info
      debug:
        msg: "Connected to {{ inventory_hostname }} ({{ ansible_host }})"