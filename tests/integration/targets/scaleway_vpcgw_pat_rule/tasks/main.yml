---
- block:
  - name: Ensure Access Key is provided
    ansible.builtin.fail:
      msg: scw_access_key should be defined in tests/integration/integration_config.yml
    when:
      - scw_access_key is not defined
      - scw_access_key | length == 0

  - name: Ensure Secret Key is provided
    ansible.builtin.fail:
      msg: scw_secret_key should be defined in tests/integration/integration_config.yml
    when:
      - scw_secret_key is not defined
      - scw_secret_key | length == 0

  - name: Create vpcgw_pat_rule
    quantumsheep.scaleway.scaleway_vpcgw_pat_rule:
      access_key: "{{ scw_access_key }}"
      secret_key: "{{ scw_secret_key }}"
      state: present
      gateway_id: "{{ resource_gateway_id }}"
      public_port: "{{ resource_public_port }}"
      private_ip: "{{ resource_private_ip }}"
      private_port: "{{ resource_private_port }}"
      protocol: "{{ resource_protocol }}"
    register: integration_vpcgw_pat_rule_creation

  - name: Create a vpcgw_pat_rule (Check)
    check_mode: yes
    quantumsheep.scaleway.scaleway_vpcgw_pat_rule:
      access_key: "{{ scw_access_key }}"
      secret_key: "{{ scw_secret_key }}"
      state: present
      gateway_id: "{{ resource_gateway_id }}"
      public_port: "{{ resource_public_port }}"
      private_ip: "{{ resource_private_ip }}"
      private_port: "{{ resource_private_port }}"
      protocol: "{{ resource_protocol }}"
    register: integration_vpcgw_pat_rule_creation_check
