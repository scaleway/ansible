---
- block:
  - name: Ensure Access Key is provided
    ansible.builtin.fail:
      msg: scw_access_key should be defined in tests/integration/integration_config.yml
    when:
      - scw_access_key is not defined
      - scw_access_key | length == 0

  - name: Ensure Secret Key is provided
    ansible.builtin.fail:
      msg: scw_secret_key should be defined in tests/integration/integration_config.yml
    when:
      - scw_secret_key is not defined
      - scw_secret_key | length == 0

  - name: Create instance_security_group
    quantumsheep.scaleway.scaleway_instance_security_group:
      access_key: "{{ scw_access_key }}"
      secret_key: "{{ scw_secret_key }}"
      state: present
      description: "{{ resource_description }}"
      stateful: "{{ resource_stateful }}"
      inbound_default_policy: "{{ resource_inbound_default_policy }}"
      outbound_default_policy: "{{ resource_outbound_default_policy }}"
    register: integration_instance_security_group_creation

  - name: Create a instance_security_group (Check)
    check_mode: yes
    quantumsheep.scaleway.scaleway_instance_security_group:
      access_key: "{{ scw_access_key }}"
      secret_key: "{{ scw_secret_key }}"
      state: present
      description: "{{ resource_description }}"
      stateful: "{{ resource_stateful }}"
      inbound_default_policy: "{{ resource_inbound_default_policy }}"
      outbound_default_policy: "{{ resource_outbound_default_policy }}"
    register: integration_instance_security_group_creation_check
